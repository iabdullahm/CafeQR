rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // For now, allow anyone to read images
      allow read: if true;
      // Allow authenticated users to write/upload images
      allow write: if request.auth != null;
    }
  }
}
